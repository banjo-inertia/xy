2013-09-25

actiondat - Perform an action on a 2D set of data.

Many scientific data sets are two dimensional: a set of abscissae and corresponding ordinates. This package provides a way to generate a script that easily performs a single action on 2D data, for example: calculating it, plotting it on the screen, or generating a graphics file of the plot.
